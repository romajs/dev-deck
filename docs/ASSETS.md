# Assets

DevDeck includes a generated app icon at `DevDeck/App/AppIcon.png` and `DevDeck/App/AppIcon.icns`.

The app icon was created specifically for this project. There are no third-party image assets checked into the repository.

Runtime badges and toolbar icons use native SwiftUI/SF Symbols at render time.
